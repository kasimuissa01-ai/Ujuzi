# Gemini API Key (Server-side)
GEMINI_API_KEY=

# Groq API Key (Server-side)
GROQ_API_KEY=

# Mixpanel Token (Client-side)
VITE_MIXPANEL_TOKEN=6cf3aaa51c63b2439e00c040e2fed861

# Supabase (Client-side)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Firebase Cloud Messaging legacy server-side key (deprecated but kept as visual optional fallback)
FCM_SERVER_KEY=

# Firebase Cloud Messaging Modern HTTP v1 Service Account Credentials (RECOMMENDED)
# Option A: Full JSON credentials block as a single string (remove line breaks or encode to base64)
FIREBASE_SERVICE_ACCOUNT=

# Option B: Individual Service Account fields (alternative to Option A)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
